# Python
__pycache__/
*.pyc
venv/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# Logs
*.log
*.old
logs/

# Terraform — provider cache, state, and generated files
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.tfvars.json
*.tfvars.json.lock

# Plaintext credentials (decrypted at runtime via SOPS)
**/credentials.auto.tfvars
.env
keys.txt

# Runtime state (generated by app)
vm_track.json
terraform/next_vmid.txt

# Claude Code
CLAUDE.md
tasks/

# Managed directories (contents are generated or sensitive)
_archive/
certificates/
playbook_templates/
